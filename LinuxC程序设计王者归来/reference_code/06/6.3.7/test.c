#include <stdio.h>
int main(void)
{
	int a = 1, b = 2, add, sub, mul, div;
	add = a + b;
	sub = a – b;
	mul = a * b;
	div = a / b; 
	printf("add %d, sub %d, mul %d, div %d\n", add, sub, mul,div);
	return 0;
}
